services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: settlement_engine
      POSTGRES_USER: settlement_engine
      POSTGRES_PASSWORD: settlement_engine
    ports:
      - "5432:5432"
    volumes:
      - settlement_engine_postgres_data:/var/lib/postgresql/data

volumes:
  settlement_engine_postgres_data:
